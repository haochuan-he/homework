// 23.10.14
// homework
// by HHC

#include <stdio.h>

int die(int n, int k);

int main()
{
    int n = 0, k = 0, ans = -1;
    scanf("%d%d", &n, &k);
    ans = die(n, k);
    printf("%d", ans);
    return 0;
}

int die(int n, int k)
{
    int people[n];
    for (int i = 0; i < n; i++)
    {
        int people[i] = 0; // 1:die
    }
}